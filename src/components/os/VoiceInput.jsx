// src/components/os/VoiceInput.jsx
// Minimal stub - needs proper implementation for OS 2.0

import React from "react";

const VoiceInput = ({ onTranscript, onComplete, disabled }) => {
  // Stub implementation - voice input functionality needs to be reimplemented
  return (
    <div className="voice-input-stub">
      {/* Voice input UI placeholder */}
    </div>
  );
};

export default VoiceInput;

